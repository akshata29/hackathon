# template/backend/app/core — COPY FROM EXAMPLE, DO NOT MODIFY
#
# This package is the base infrastructure layer shared by every use-case.
# It provides: auth, conversation/session management, guardrails, observability, and
# generic HTTP routes.
#
# To use: copy this entire core/ directory into your project's backend/app/core/.
# Your app/config.py must define a Settings class with the core fields listed in
# template/backend/app/config.py.
