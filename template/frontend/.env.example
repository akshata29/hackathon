# Azure Entra ID (MSAL) - required for authentication
VITE_ENTRA_CLIENT_ID=your-entra-app-client-id
VITE_ENTRA_TENANT_ID=your-entra-tenant-id

# Backend URL (leave empty to use same origin in production; set for local dev)
VITE_BACKEND_URL=http://localhost:8000
